import { TextField } from "@/shared/ui/form/FormKit";

/**
 * `Field` é o nome histórico deste input em várias telas. Mantido como alias do
 * `TextField` do FormKit para que todos os formulários compartilhem exatamente
 * a mesma casca, rótulo e linha de erro.
 */
const Field = TextField;

export default Field;
